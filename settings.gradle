include ':smuggler-runtime'
include ':smuggler-compiler'
include ':smuggler-plugin'

if (!hasProperty("skipSample")) {
  include ':smuggler-samples:smuggler-tests'
  include ':smuggler-samples:smuggler-application'
  include ':smuggler-samples:smuggler-library'
}
